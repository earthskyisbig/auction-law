# auction-law — 환경변수(비밀) 예시
# 이 파일을 .env 로 복사하고 실제 값을 채운다. .env 는 절대 커밋하지 않는다(.gitignore 처리됨).

# 국가법령정보 공동활용(law.go.kr) Open API 인증
# OC = open.law.go.kr 등록 이메일 아이디(앞부분). 호출 서버 공인 IP도 사이트에 등록 필수. 미설정 시 test로 폴백.
LAW_OC=

# DuckDB 경로 override (기본 data/app.duckdb)
# DB_PATH=
