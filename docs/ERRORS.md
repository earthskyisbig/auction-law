# 에러 재발방지 로그 — auction-law

> **같은 에러를 두 번 겪지 않기 위한 문서.**
> - 작업 시작 시 이 문서를 먼저 읽는다.
> - 에러를 해결할 때마다 아래 표에 한 줄 추가한다(증상·원인·해결·재발방지).
> - 재발방지 열에는 "다음엔 이렇게 하면 안 겪는다"를 적는다.
> 최초 생성: 2026-07-14

| 날짜 | 증상 | 원인 | 해결 | 재발방지 |
|------|------|------|------|----------|
| 2026-07-14 | law.go.kr API가 `{"result":"사용자 정보 검증에 실패하였습니다."}` 반환 (OC=myung7788) | OC는 맞으나 **호출 서버 공인 IP가 미등록**. law.go.kr은 OC + IP를 함께 검증 | open.law.go.kr 마이페이지 > OPEN API 신청관리에서 호출 IP 등록 (반영 지연 수 분~시간) | 오류 msg에 "IP주소/도메인 등록"이 있으면 OC가 아니라 **IP 문제**. 유동 IP는 변경 시 재등록. `curl -s https://api.ipify.org`로 현재 IP 확인 |
| 2026-07-14 | law.go.kr `target=expc` 본문검색(search=2)이 무관한 결과만 반환 | ~~test OC의 expc 본문검색 제한~~ **(2026-07-15 오진으로 판명 — 아래 행 참조)** | `search=1`(안건명) 목록검색 사용 | — |
| 2026-07-15 | `search=2`(본문검색)가 **target 불문** 무관한 결과 반환 (prec에 "유치권 경매" → 음주운전·해고무효확인이 나옴) | **OC 문제 아님.** 등록 OC(myung7788)로도 동일 → law.go.kr API의 `search=2`가 사실상 동작하지 않음. 전날 "test OC 제한"으로 오진했던 것을 실측으로 반증 | **`search=1`(기본, 제목·사건명 검색)만 사용.** 키워드를 바꿔가며 재검색하고, 본문은 `body --target ... --id`로 확인 | `search=2`를 폴백으로 쓰지 말 것 — 조용히 엉뚱한 근거를 물어온다. 검색 실패 시 **키워드 변형**이 정답. 원인을 못 찾았을 때 "OC/권한 탓"으로 성급히 결론내지 말고 **대조군(등록 OC vs test, target별)으로 반증**할 것 |
| 2026-07-27 | `Agent` 호출 시 `isolation: "worktree"`로 statute-researcher·precedent-researcher를 스폰했더니, 완료 후 조사 결과 파일(`_workspace/01_statute_findings.md` 등)이 통째로 사라짐 | `_workspace/`가 `.gitignore` 대상 → 격리 워크트리 안에서 그 폴더에 파일을 써도 git이 "변경 없음"으로 판정 → 에이전트 종료 시 워크트리 자동 정리(삭제)되며 안에 쓴 파일도 같이 삭제됨 | 에이전트가 완료 보고서 본문에 남긴 조사 내용을 근거로 파일을 수동 재구성. 이후 재발 방지책을 `realestate-law-orchestrator/SKILL.md`에 반영(조사관 스폰 시 `isolation` 파라미터 사용 금지) | `_workspace/`처럼 gitignore된 디렉터리에 결과물을 남기는 에이전트는 **worktree로 격리하지 말 것**(격리는 코드 변경 작업에만 쓸 것). 격리 스폰 후 결과 확인 전 `git worktree list`로 워크트리가 아직 살아있는지부터 체크 |
| 2026-07-27 | 신규 target `licbyl`(법령 별표·서식) 추가 후 `body --target licbyl --id ...` 호출 시 JSON도 아니고 에러도 아닌, 목록 뷰어용 HTML 위젯 조각이 그대로 돌아옴 | law.go.kr이 `licbyl`의 `lawService.do`(본문 조회)에 대해 `type=JSON`을 사실상 무시하고 항상 HTML 조각을 반환 — `[ERROR:PARSE_FAILED]` 마커는 찍히지만 원인이 OC/IP 문제가 아니라 **이 target 자체가 body 조회를 지원 안 함** | `body` 호출을 아예 하지 않는 것으로 정정 — `search` 응답에 이미 `별표서식파일링크`/`별표서식PDF파일링크`(다운로드 URL)가 포함돼 있으므로 그걸 그대로 쓴다 | 새 target을 추가할 때 search만 실측하고 끝내지 말 것 — **body(본문 조회)까지 실제로 호출해 응답 형태를 확인**한 뒤에야 조사관 문서에 사용법을 적을 것. `[ERROR:PARSE_FAILED]`가 떠도 "OC 문제"로 단정하지 말고 그 target이 애초에 JSON 본문 조회를 지원하는지부터 의심할 것 |
| 2026-08-10 | 세법 위키 조사 중 `target=prec`로 조세 관련 하급심 판례를 목록(search)에서는 찾았는데, `body --target prec --id <일련번호>`로 본문을 조회하면 `{"Law": "일치하는 판례가 없습니다."}`가 돌아옴(예: 일련번호 346006, 351362, 617869) | 해당 판례들의 데이터 출처가 "국세법령정보시스템"(국세청)이라 law.go.kr 자체 판례 DB(`lawService.do`)에는 본문이 없음 — **목록(lawSearch)과 본문(lawService)의 데이터 소스가 target 내에서도 갈릴 수 있다는 뜻**, `[ERROR:...]` 마커가 뜨는 게 아니라 정상 JSON 응답으로 "없다"고 답하기 때문에 자동 감지가 안 됨 | 본문을 못 가져온 판례는 **목록의 "사건명" 필드 원문만** 인용하고, "판시요지·이유는 확인 안 됨(본문 조회 실패, 출처가 국세법령정보시스템으로 추정)"을 명시. 실제 이유가 필요하면 국세법령정보시스템(taxlaw.nts.go.kr)에서 별도 확인 | 판례 본문 조회 결과가 `{"Law": "일치하는 판례가 없습니다."}`처럼 **정상 JSON이지만 내용이 없는 형태**로 오면 `[NOT_FOUND]`/`[ERROR:*]` 마커가 안 찍히므로 조사관이 직접 이 문구를 감지해야 한다 — 특히 조세·행정 하급심처럼 원 출처가 법원이 아닌 판례에서 재현되기 쉽다 |

<!--
작성 예시:
| 2026-07-14 | 텔레그램 봇 HTTP 409 반복 | 같은 봇 토큰을 다른 곳에서도 getUpdates 폴링 중(봇당 폴러 1개만 허용) | 전용 봇 토큰 신규 발급 | 봇마다 토큰 분리. 발송(sendMessage)은 공유 무해, 수신(getUpdates)만 배타적임을 기억 |
| 2026-07-14 | 5천건 병합 2분 타임아웃 | 제목 유사도 O(n²) 전량 비교 | 정규화 제목 접두어 버킷팅 후 버킷 내 비교 | 근사 중복제거는 블로킹으로 후보를 먼저 좁힌다 |
-->
